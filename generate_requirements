# Generate requirements.txt for current directory
pipreqs .

# Specify project path
pipreqs /path/to/project

# Overwrite existing requirements.txt
pipreqs . --force

# Include only packages from pypi
pipreqs . --pypi-server https://pypi.org/simple/







